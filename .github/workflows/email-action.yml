name: Email Step Action

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: 24ds2000054@ds.study.iitm.ac.in
        run: echo "Hello from GitHub Actions with my email as the step name!"
